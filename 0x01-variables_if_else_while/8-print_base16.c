#include <stdio.h>
/**
* main - Entry point
*
* Return: Always 0 (Success)
*/
int main(void)
{
	int a = 48;
	int m = 97;

	while (a < 58)
	{
		putchar (a);
		a++;
	}
	while (m < 103)
	{
		putchar (m);
		m++; }
	putchar('\n');
	return (0);
	}
